<!DOCTYPE html>
<html>
  <head>
  <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" href="https://unpkg.com/@freshworks/crayons@v4/css/crayons-min.css"/>
    <style>
  @import url("https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500;700&display=swap");

  * {
    font-family: "Ubuntu", sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  a{
      color:inherit;
      text-decoration:none;
  }
  label{
   margin:25px 0px;
  }

input[type=email], input[type=password] {
  width: 100%;
    padding:10px;
    height:40px;
    margin:10px 0px;
  }

.accept{
    background-color:#50C878;
    height:32px;
    color: white; 
    font-size: 16px; 
    border: none; 
    border-radius: 5px; 
    cursor: pointer;
    width:122px;
}
.reject{
    background-color:#FF3333 ;
    height:32px;
    color: white; 
    font-size: 16px; 
    border: none; 
    border-radius: 5px; 
    cursor: pointer;
    width:122px;
  margin-top:10px;
}
.field{
  padding:15px 0px;
}
.field label{
  padding:15px 0px;
}
.remember{
  padding:15px 0px;
}
.forgot-pass{
  padding: 27px 0px;
  float: right;
}
.signup-btn{
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: 20px;
}
.task {
  cursor: move;
  background-color: var(--white);
  padding: 1rem;
  border-radius: 8px;
  width: 100%;
  box-shadow: rgba(99, 99, 99, 0.1) 0px 2px 8px 0px;
  margin-bottom: 1rem;
  border: 3px dashed  transparent;
}

.task:hover {
  box-shadow: rgba(99, 99, 99, 0.3) 0px 2px 8px 0px;
  border-color: rgba(162, 179, 207, .2)!important;
}

.task p {
  font-size: 15px;
  margin: 1.2rem 0;
}

.task__tag {
  border-radius: 100px;
  padding: 2px 13px;
  font-size: 12px;
}
.task__stats {
  margin-top:20px;
  position: relative;
  width: 100%;
  color: var(--light-grey);
  font-size: 12px;
}
  </style>
  </head>
  <body>
<h2>Your Invitations</h2>


 
 


<% @invitations.each do |invitation| %>
<div class='task' >
<h2> <%= "#{invitation.sender.email} has invited you to join #{invitation.project.project_name}" %></h2>
<div class='task__stats'>
<%= button_to 'Accept', accept_invitation_path(invitation), method: :post , class: 'accept' %>
<%= button_to 'Reject', reject_invitation_path(invitation), method: :delete , class: 'reject' %>
</div>
</div>
<% end %>
</body>
</html>