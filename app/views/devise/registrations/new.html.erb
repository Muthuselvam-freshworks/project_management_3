<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrations</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    <link rel="stylesheet" href="https://unpkg.com/@freshworks/crayons@v4/css/crayons-min.css"/>
    <style>
    
    body {
      background-color:rgba(18, 52, 77, 0.5);
    }
    form {
      margin-top:100px;
      background-color: #fff;
    }
    .text-indigo-600{
      color:#000;
    }
    .bg-indigo-600{
      background-color:#50C878;
    }

    .top{
      margin-top:100px;
    }
    .login{
      position: absolute;
    left: 45%;
    margin: 13px 0px;
    }
    </style>
    
</head>
<body>


<div class="mx-auto max-w-screen-xl px-4 py-16 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-lg top">
   

    

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'mb-0 mt-6 space-y-4 rounded-lg p-4 shadow-lg sm:p-6 lg:p-8' }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <p class="text-center text-lg font-medium">Sign up for your account</p>

      <div class="field">
        <%= f.label :first_name %><br />
        <%= f.text_field :first_name, class: 'w-full rounded-lg border-gray-200 p-4 pe-12 text-sm shadow-sm', placeholder: 'Enter first name', autofocus: true , required: true , pattern: "[^\s]+" , title: "No spaces allowed"%>
      </div>

      <div class="field">
        <%= f.label :last_name %><br />
        <%= f.text_field :last_name, class: 'w-full rounded-lg border-gray-200 p-4 pe-12 text-sm shadow-sm', placeholder: 'Enter last name' , required: true , pattern: "[^\s]+" , title: "No spaces allowed"%>
      </div>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, class: 'w-full rounded-lg border-gray-200 p-4 pe-12 text-sm shadow-sm', placeholder: 'Enter email', autocomplete: 'email' , required: true , pattern: '[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$'%>
      </div>

      <div class="field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, class: 'w-full rounded-lg border-gray-200 p-4 pe-12 text-sm shadow-sm', placeholder: 'Enter password', autocomplete: 'new-password' , required: true , pattern: '^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{6,}$' , title: "Minimum six characters, at least one letter and one number"%>
      </div>

      <div class="field">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, class: 'w-full rounded-lg border-gray-200 p-4 pe-12 text-sm shadow-sm', placeholder: 'Confirm password', autocomplete: 'new-password' , required: true , pattern: '^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{6,}$', title: "Minimum six characters, at least one letter and one number" %>
      </div>

      <div class="actions">
        <%= f.submit "Sign up", class: 'block w-full rounded-lg bg-indigo-600 px-5 py-3 text-sm font-medium text-white' %>
      </div>
      <%- if controller_name != 'sessions' %>
      <%= link_to "Already a member? Log in", new_session_path(resource_name) , class: 'login' %><br />
    <% end %>
    <% end %>
    
  </div>
</div>
<script type="module" src="https://unpkg.com/@freshworks/crayons@v4/dist/crayons/crayons.esm.js"></script>
<script nomodule src="https://unpkg.com/@freshworks/crayons@v4/dist/crayons/crayons.js"></script>
</body>
</html>